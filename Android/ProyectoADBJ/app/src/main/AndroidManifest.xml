<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoadbj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".Dashboard"></activity>
        <activity android:name=".menuPago" />
        <activity android:name=".menuDatosUsuario" />
        <activity android:name=".menuRegistro" />
        <activity android:name=".menuPlanes" />
        <activity android:name=".menuInfo" />
        <activity android:name=".menuPrincipal" />
        <activity android:name=".MainActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            android:theme="@style/AppTheme.NoActionBar"
        </activity>
    </application>

</manifest>